<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sch.id.smkn1bangsri.jeparaadventure">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_new_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".DetailReligiActivity" />
        <activity android:name=".ReligiActivity" />
        <activity android:name=".DetailKulinerActivity" />
        <activity android:name=".DetailWisataActivity" />
        <activity android:name=".DetailBudayaActivity" />
        <activity android:name=".KulinerActivity" />
        <activity android:name=".WisataActivity" />
        <activity android:name=".BudayaActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>